http:
  address:         0.0.0.0:8080
  workers:
    command:  "php psr-worker.php"
    pool:
      numWorkers: 1
      maxJobs:  1

static:
  enable:  true
  dir:   "public"
  forbid: [".php", ".htaccess"]